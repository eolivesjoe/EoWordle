<UserControl x:Class="EoWordle.Views.GameView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:EoWordle.Views">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid x:Name="GuessesGrid" Grid.Row="0" Margin="10"/>

        <TextBox Grid.Row="5" Margin="10" VerticalAlignment="Bottom" KeyDown="GuessTextBox_KeyDown"/>
    </Grid>
</UserControl>